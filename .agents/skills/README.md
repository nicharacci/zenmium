# Skills catalog

- `caostack` overlay lives in `caostack/skills/caostack`. `bash caostack/install.sh` links it here.
- `poteto-mode` comes from vendored pstack (gitignored `vendor/pstack`).
- Ponytail, shadcn-cssinjs, impeccable, clipth: install from `caostack/lock.json` in Goalpost Code or Cursor.
- Do not vendor solvys-skills.
